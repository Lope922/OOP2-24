Public Class ResolvedTickets

    Private Sub lstBoxResolvedTickets_SelectedIndexChanged(sender As Object, e As EventArgs) Handles lstBoxResolvedTickets.SelectedIndexChanged
        ' within this listbox i need to add the tickets to the that have been resolved in the same format they are written to the other list box 
        'may be useful to utilize a structure. 

    End Sub
End Class